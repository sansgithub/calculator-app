import React from 'react';

const ComputeComponent = (props) =>(
    
            <div className='compute-screen'>
                {props.children}
            </div>
        );

export default ComputeComponent;